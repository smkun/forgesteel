# Local Development Backend Configuration
# This file is used by the backend when running in development mode

# MySQL Connection (Local)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=forgesteel
DB_USER=root
DB_PASS=q1w2e3r4t5y6

# Firebase Admin SDK (local path)
GOOGLE_APPLICATION_CREDENTIALS=./firebase-service-account.json

# CORS Origins (comma-separated)
ALLOWED_ORIGIN=http://localhost:5173

# Environment
NODE_ENV=development

# Logging
LOG_LEVEL=debug

# Local development port
PORT=4000
