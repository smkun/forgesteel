# Forgesteel Backend API Environment Configuration
# Copy this file to .env.local and fill in your values
# NEVER commit .env.local to git (it's in .gitignore)

# ==================================================================
# DATABASE CONFIGURATION
# ==================================================================

# MySQL connection string
# Format: mysql://username:password@hostname:3306/database
# Example: mysql://forgesteel_user:password123@sql123.ifastnet.com:3306/forgesteel_db
MYSQL_URL=mysql://username:password@hostname:3306/database

# ==================================================================
# FIREBASE ADMIN SDK CONFIGURATION
# ==================================================================

# Path to Firebase service account JSON file
# Download from Firebase Console → Project Settings → Service Accounts
# Store in a secure location outside of git (e.g., ~/.config/firebase/)
GOOGLE_APPLICATION_CREDENTIALS=/path/to/firebase-service-account.json

# Alternative: Inline Firebase credentials (use if file path not available)
# Uncomment and fill these if you can't use GOOGLE_APPLICATION_CREDENTIALS
# FIREBASE_PROJECT_ID=your-project-id
# FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
# FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\n...\n-----END PRIVATE KEY-----\n"

# ==================================================================
# CORS CONFIGURATION
# ==================================================================

# Allowed origins for CORS (comma-separated)
# Development: Include localhost ports
# Production: Use your production domain
ALLOWED_ORIGIN=http://localhost:5173,http://localhost:4173,https://yourdomain.com

# ==================================================================
# SERVER CONFIGURATION
# ==================================================================

# CRITICAL: DO NOT SET PORT FOR PASSENGER DEPLOYMENTS
# Passenger manages port automatically - setting PORT causes conflicts
# Reference: PASSENGER_DEPLOYMENT_GUIDE.md section "Common Pitfalls"

# Uncomment ONLY for non-Passenger deployments (e.g., Vercel, Railway)
# PORT=3000

# Node environment (development, production)
NODE_ENV=development

# ==================================================================
# SECURITY (DEVELOPMENT ONLY)
# ==================================================================

# DANGER: Development auth bypass
# Set to "true" to skip Firebase authentication (local dev ONLY)
# NEVER enable in production - security vulnerability
# DEV_AUTH=false

# ==================================================================
# LOGGING
# ==================================================================

# Log level (error, warn, info, debug)
LOG_LEVEL=info

# ==================================================================
# DEPLOYMENT NOTES
# ==================================================================

# iFastNet Passenger Deployment:
# - Store this file at: /home/username/nodejs/forgesteel-api/.env.local
# - Ensure file permissions: chmod 600 .env.local
# - DO NOT set PORT variable
# - Use absolute paths for GOOGLE_APPLICATION_CREDENTIALS
# - Set ALLOWED_ORIGIN to production domain

# Local Development:
# - Place at project root: /path/to/forgesteel/.env.local
# - Can use relative paths
# - Include localhost in ALLOWED_ORIGIN
